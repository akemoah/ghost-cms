---
- hosts: local
  #remote_user: ansible
  become: yes
  #become_method: sudo
  tasks:
    - name: Create a tgz archive of ghost-cms
      archive:
        path: ./workspace/compressjob
        remove: yes
        #dest: ./workspace/compressjob.tgz
        #force_archive: true
        
