---
- hosts: local
  remote_user: vagrant
  become: yes
  become_method: sudo
  tasks:
    - name: Create a zip archive of ghost-cms
      archive:
        path: /home/vagrant/ghost-cms
        dest: /home/vagrant/ghost-cms.tgz
