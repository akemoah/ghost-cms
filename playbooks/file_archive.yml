---
- hosts: local
  remote_user: vagrant
  become: yes
  become_method: sudo
  tasks:
    - name: Create a zip archive of ghost-cms
      archive:
        path: ./ghost-cms
        dest: ./ghost-cms.gz
        #format: gz
        #force_archive: true
        
