---
- hosts: local
  remote_user: ansible
  become: yes
  become_method: sudo
  tasks:
    - name: Create a zip archive of ghost-cms
      archive:
        path: ./ghost-cms
        dest: ./ghost-cms.gz
        #format: gz
        #force_archive: true
        
